# Static Profile Data (SPD) Files

## Current SPD files

The `map.spd` file in this directory was extracted from a regular (non-instrumented)
build of the `map.sys` native driver. It serves as a baseline for SPGO.
